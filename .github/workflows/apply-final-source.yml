name: Apply final source for pull request 31

on:
  pull_request:
    branches:
      - agent/pr31-staging-base
    paths:
      - .github/final-patch-clean/READY

permissions:
  contents: read

jobs:
  waiting:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo waiting
