diff --git a/src/config.ts b/src/config.ts
index 2c32a3c..9d8f0a1 100644
--- a/src/config.ts
+++ b/src/config.ts
@@ -460,8 +460,8 @@ function createMergedBrowserConfig(
       ...pickDefined(overrides.browser?.contextOptions),
     },
     cdpHeaders: {
-      ...(base.browser.cdpHeaders ?? {}),
-      ...(overrides.browser?.cdpHeaders ?? {}),
+      ...pickDefined(base.browser.cdpHeaders),
+      ...pickDefined(overrides.browser?.cdpHeaders),
     },
     cdpTimeout:
       overrides.browser?.cdpTimeout ??
@@ -573,7 +573,6 @@ function envToBoolean(value: string | undefined): boolean | undefined {
   if (value === 'false' || value === '0') {
     return false;
   }
-  return;
 }
 
 function envToString(value: string | undefined): string | undefined {
